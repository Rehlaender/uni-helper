
<div class="jumbotron col-lg-6 col-md-offset-3">
  <h1 align="center">Bienvenido!</h1>
  <h3 align="center">"<%= current_user.name %>"</h3>
  <br>

  <div align="center">
    <p>
      ¿Hoy vamos a?
    </p>
    <p>
      <!-- falta validacion en cuanto a nombre segun la base de datos
      <a class="btn btn-material-cyan" href="<%= posts_category_post_path(2) %>"><%= CategoryPost.find(2).name %></a>
      -->
      <a class="btn btn-material-cyan" href="<%= posts_category_post_path(1) %>">Vender</a>
      <%= link_to 'Postear' , new_post_path, :class=>"btn btn-material-red" %>      
      <a class="btn btn-material-cyan" href="<%= posts_category_post_path(2) %>">Comprar</a>
    </p>
  </div>
</div>
