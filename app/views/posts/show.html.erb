<p id="notice"><%= notice %></p>

<table class="table table-striped table-hover">
  <thead>
    <tr>
      <!--<th>ID</th>-->
      <th>Posteador</th>
      <th>Descripcion</th>
      <th>Orientado a la facultad</th>
      <th>Categoria</th>
      <th>Costo</th>
      <!--<th>Category_post</th>-->
    </tr>
  </thead>
  <tbody>
    <tr class="info">
      <!--<td><%= @post.id %></td>-->
      <!--<td><%= @post.user.name %></td>-->
      <td><a href='<%= user_path(@post.user.id)%>'> <%= @post.user.name %></a></td>
      <td><%= @post.description %></td>
      <td><%= @post.school.name %></td>
      <td><%= @post.category.name %></td>
      <td><%= @post.price %></td>
      <!--<td><%= @post.category_post.name %></td>-->
    </tr>
  </tbody>
</table>


<p>
  <a class="" href="<%= posts_category_post_path(2) %>">Regresar</a>
  <!-- Este metodo de "Back" es una mexicanada, cambiar cuando se resuelva el loop de 2 link_to :back #jeje
  <%= link_to 'Regresar', :back %> -->
</p>
<!--
<%= link_to 'Edit', edit_post_path(@post) %> |

-->
