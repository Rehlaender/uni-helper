<p id="notice"><%= notice %></p>

  <h1 class="text-center" id="pdf_nel">Administrador: <%= @admin.name %></h1>
  <div class="container-fluid">
    <div class="row" id="pdf_nel">
      <div class="jumbotron col-md-10 col-sm-10 col-sm-offset-1" id="">
        <%= render 'buttons' %>
      </div>
    </div>
    <div class="row">
      <div class="jumbotron col-md-offset-1 col-md-10 col-sm-10 col-sm-offset-1 " id="reportes">
        <%= render 'posts' %>
        <%= render 'events' %>
      </div>
    </div>
  </div>
